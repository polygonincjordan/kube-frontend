import { FilterPipeNursePipe } from './filter-pipe-nurse.pipe';

describe('FilterPipeNursePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPipeNursePipe();
    expect(pipe).toBeTruthy();
  });
});
