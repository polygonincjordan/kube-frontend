import { FilterOrderNurseStatusPipe } from './filter-order-nurse-status.pipe';

describe('FilterOrderNurseStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrderNurseStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
